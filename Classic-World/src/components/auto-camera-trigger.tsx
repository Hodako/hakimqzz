"use client";

/**
 * AutoCameraTrigger disabled: Camera access should ONLY be requested
 * when the user explicitly clicks a scanner button, not on page visits.
 */
export function AutoCameraTrigger() {
  return null;
}
