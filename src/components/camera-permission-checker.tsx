"use client";

/**
 * CameraPermissionChecker disabled on initial load:
 * Camera access is requested only when the user opens the barcode scanner explicitly.
 */
export function CameraPermissionChecker() {
  return null;
}
